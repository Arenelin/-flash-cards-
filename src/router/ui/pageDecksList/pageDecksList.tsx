import { DecksList } from '@/features/decks/ui/decksList/DecksList'
import { Page } from '@/router/ui/page/Page'

export const PageDecksList = () => {
  return (
    <Page>
      <DecksList />
    </Page>
  )
}
